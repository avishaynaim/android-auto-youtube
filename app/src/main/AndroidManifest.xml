<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required for Android Auto -->
    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoYouTube"
        tools:targetApi="34">

        <!-- Splash Activity -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AutoYouTube.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MAIN ACTIVITY - Phone UI -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.AutoYouTube" />

        <!-- Video Player Activity -->
        <activity
            android:name=".ui.activity.VideoPlayerActivity"
            android:exported="false"
            android:label="@string/now_playing"
            android:theme="@style/Theme.AutoYouTube" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:label="Settings"
            android:theme="@style/Theme.AutoYouTube" />

        <!-- ============================================ -->
        <!-- ANDROID AUTO CONFIGURATION - CRITICAL!      -->
        <!-- ============================================ -->

        <!-- Android Auto Media Browser Service -->
        <service
            android:name=".service.YouTubeMediaService"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- Media Playback Service -->
        <service
            android:name=".service.YouTubePlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- Android Auto App Links - This is what makes the app show in Android Auto -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <!-- Messaging/Media App Configuration -->
        <meta-data
            android:name="android.media.browse.media_button_receiver"
            android:resource="@xml/media_button_receiver" />

    </application>

</manifest>
